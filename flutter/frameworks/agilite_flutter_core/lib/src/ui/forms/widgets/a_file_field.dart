import 'package:flutter/material.dart';

class AFileField extends StatelessWidget {
  const AFileField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
